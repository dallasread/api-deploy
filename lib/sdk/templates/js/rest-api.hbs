var API = {
    debug: false,

{{#each methods}}
    {{#if x-api-deploy}}
        {{#if x-api-deploy.sdk.method}}
    {{x-api-deploy.sdk.method}}: function {{x-api-deploy.sdk.method}}(data, done) {
    {{#if x-amazon-lambda.arn}}
        $.ajax({
            method: '{{_method}}',
            url: '{{../sdk.url}}{{_path}}',
            data: data,
            complete: done
        });
    {{else}}
        this.debug && console.warn('Api Gateway Endpoint not deployed:', '{{x-api-deploy.sdk.method}}');
    {{/if}}
    },
        {{/if}}
    {{/if}}
{{/each}}
};

if (window) window.{{sdk.name}} = API;
module.exports = API;
